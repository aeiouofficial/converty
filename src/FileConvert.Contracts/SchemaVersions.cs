namespace FileConvert.Contracts;

/// <summary>Schema versions shared by persisted and wire-facing FileConvert contracts.</summary>
public static class SchemaVersions
{
    public const int Current = 1;
}
