CONVERTY — FRESH-ACCOUNT AGENT HANDOVER

READ THIS ENTIRE FILE BEFORE CHANGING CODE.

Repository: https://github.com/aeiouofficial/converty
Product: Converty, a Windows 11 Explorer modern-context-menu converter.
Default branch: main.
Current development line: 0.1.0-dev.19 Image mixed-batch isolation.

MISSION
Finish Converty to customer-shippable quality. Work directly in GitHub. Do not merely propose work. Commit every meaningful change, run the repository's real CI, preserve deterministic generated authority, and leave the repo curated and self-explanatory for the next agent.

CURRENT AUTHORITY
Dev.18 is frozen on main at commit ef079f7e7923e399624067c4d54b9ce7577bf090, tree 0af729f150897d170eac9f9aebfd5bc7d5d4083a. Exact-main CI run 33390111824 passed continuity, supply-chain/static and Windows managed qualification. It included 254/254 managed tests, 95/95 static tests, 5/5 contract vectors, 36/36 Audio source/action conversions, repeated malformed/truncated Audio rejection, Audio mixed-batch isolation, and 24/24 Image source/action conversions plus repeated malformed/truncated Image rejection.

ARCHITECTURE — DO NOT WIDEN TRUST BOUNDARIES
IExplorerCommand DLL → fixed app-local Converty.Bridge.exe → strict disposable Converty.EngineWorker.exe / typed provider → fixed app-local FFmpeg → private staging → validated transactional numbered no-overwrite publication.

Host/Bridge are media/process neutral. Untrusted parsing/codec execution belongs in the disposable restricted worker/provider. Never add shell command construction, raw FFmpeg argument passthrough, arbitrary converter executable paths, PATH lookup, writable plugin/DLL discovery, ordinary conversion network dependencies, or silent Strict→Compatibility fallback.

Preserve Unicode/metacharacter filenames, source files, existing destinations, bounded waits, worker termination, transactional publication and current-user IPC authentication ordering. Signing private keys never enter repo/workspace. GitHub Actions external actions use exact full SHA pins and normal CI contents read-only with checkout persist-credentials=false.

DEV.19 IMPLEMENTATION
Branch: dev/0.1.0-dev.19-image-batch-isolation.
Implemented files:
- build/image-batch-isolation-smoke.ps1 — real Windows packaged mixed Image product gate.
- tests/static/test_dev19_image_batch_isolation.py — static contract.
- tests/Converty.Core.Tests/Execution/ImageBatchIsolationTests.cs — focused core-runner coverage.
- .github/workflows/ci.yml — managed Image mixed-batch gate.
- docs/development/DEV19_IMAGE_BATCH_ISOLATION_TDD_EVIDENCE.md
- docs/superpowers/specs/2026-09-02-dev19-image-batch-isolation-design.md
- docs/superpowers/plans/2026-09-02-dev19-image-batch-isolation.md

Behavior contract: one same-family Image selection is valid → malformed → valid → truncated → valid. One Bridge process handles the batch. Ordinary per-file failures do not suppress later valid members. Aggregate failure is reported after all members are attempted. Later valid outputs publish with numbered/no-overwrite semantics; failing members publish nothing. Sources and pre-existing destinations remain byte-identical. No .converty-*.partial.* residue and no test-package converter-worker/FFmpeg orphan processes remain. The complete batch is repeated twice.

TDD HISTORY
The dev.19 static RED was established before the acceptance smoke existed. During implementation two harness defects were found and corrected with focused regression checks: PowerShell $attempt: interpolation and inline array concatenation passed as -Arguments. Do not remove those guards or weaken assertions to hide harness failures.

CURRENT CI QUALIFICATION
A latest branch run may still be in progress. Always re-fetch GitHub before acting. Side-branch continuity failure is expected. Static must reach zero-diff; managed must complete the complete Windows path, including Audio and Image regressions, Image mixed batch, managed tests and deterministic workspace packaging.

AUTHORITY FREEZE SEQUENCE
1. Finish behavior qualification.
2. Set VERSION to 0.1.0-dev.19 and curate README/status/backlog/changelog/machine-readable evidence/handover while preserving all historical granular evidence keys.
3. Generate source SBOM, release SBOM, package manifest and SHA256SUMS only through CI.
4. Independently verify the generated artifact digest, exact four-member set, CRC and version.
5. Use a guarded exact-parent temporary sync workflow; it must touch only those four generated files plus self-delete.
6. Require branch zero-diff qualification.
7. Re-read live main.
8. Fast-forward main non-force only if the expected base is unchanged.
9. Require a fresh exact-main CI run with continuity + supply-chain/static + managed all SUCCESS.
10. Independently verify exact-main verified delivery and deterministic workspace; re-read both refs and confirm exact SHA/tree.

REPOSITORY CURATION
Source tests remain under tests/. Generated build/test/media/package/log output stays under excluded artifacts/, bin/, obj/ and caches. Never hand-edit generated SBOM/package/hash files. Remove temporary workflow files from final trees. Preserve historical changelog/evidence fields. If GitHub connector permissions prevent deleting stale noncanonical refs, do not force or rewrite them; document the limitation.

NEXT PRODUCT ORDER — DEV.20 VIDEO
After dev.19 is frozen, create `dev/0.1.0-dev.20-video-foundation` from the exact frozen main. Work test-first:
- inspect and reuse the existing typed registry/provider/native architecture;
- define fixed typed Video action IDs and output contracts;
- establish RED static and managed tests before implementation;
- qualify representative Video source extensions against every fixed action;
- preserve Unicode/metacharacter filenames, pre-existing destinations and collision numbering;
- add malformed/truncated deterministic rejection;
- prove packaged Bridge → Strict Worker/provider → app-local FFmpeg;
- prove mixed-valid/invalid Video batch isolation using the established runner behavior;
- prove worker/process cleanup, staging cleanup and bounded waits;
- retain all Audio 36-case + malformed/truncated + mixed-batch and Image 24-case + malformed/truncated + mixed-batch regression gates.

RELEASE/HARDENING AFTER VIDEO
1. Video closure.
2. Additional justified fixed media families.
3. UX/settings: defaults, mixed-selection UX, progress/results, output/concurrency/isolation settings.
4. Plugin SDK manifest/API/signature/hash gate with worker-only sample provider.
5. Production FFmpeg/ffprobe exact binaries, provenance, signatures, hashes, license/notices and redistribution approval.
6. Production signed-package B2 identity/authentication requalification.
7. Signed production MSIX clean Windows 11 install/update/uninstall.
8. Headed Windows 11 modern Explorer exact-build mouse-driven acceptance, screenshots and crash/hang/failure matrix.
9. Final fuzz/chaos/security/release audit.
10. Final SBOM/notices/hash/delivery audit and headed end-user acceptance.
11. Only then declare customer shipment readiness.

NON-NEGOTIABLE LIMITATIONS
Automated CI does not prove headed Windows 11 Explorer UX, production signing, production FFmpeg redistribution approval, signed MSIX lifecycle, or final end-user/security acceptance. Keep those explicitly OPEN until their dedicated evidence exists. Never claim absolute regression-free status.

FIRST ACTIONS FOR A NEW AGENT
1. Fetch live main and dev/0.1.0-dev.19-image-batch-isolation plus latest CI runs.
2. Determine actual current heads; GitHub is authoritative over this document.
3. Finish dev.19 qualification and freeze if not already frozen.
4. Keep every change committed and every generated authority update CI-derived.
5. Create dev.20 only from the exact frozen main commit.
6. Begin dev.20 RED-first and preserve all prior regression gates.
7. End every completed tranche by updating this handover recursively with exact refs, commits, tests, artifacts, hashes, blockers, unverified claims and one precise next task.

DO NOT RESET OR FORCE-PUSH MAIN. DO NOT DELETE HISTORICAL COMMITS. DO NOT PUT SECRETS OR PRIVATE SIGNING MATERIAL IN THE REPOSITORY.
