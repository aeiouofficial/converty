# Converty Continuation Prompt

Continue the Converty project from workspace `0.1.0-dev.5`.

Treat the repository contents as the source of truth. Read first:
1. `docs/HANDOVER_NEXT_AGENT.md`
2. `machine-readable/handover_state.json`
3. `machine-readable/build_evidence.json`
4. `docs/development/IMPLEMENTATION_STATUS.md`
5. `docs/TASK_BACKLOG.md`
6. `docs/Converty_Master_Build_Plan.md`
7. `docs/SECURITY_THREAT_MODEL.md`
8. `docs/TEST_AND_RELEASE_GATES.md`

The B0/B1 managed gate is qualified: seven locked projects restore, the real dependency audit reports zero vulnerable-result packages, Release build has zero warnings/errors, 63/63 managed tests pass, and native topology smoke passes. Do not redo or weaken those gates; preserve them as regression authority.

The next delivered workspace is `0.1.0-dev.6` and the next batch is B2 Host/Bridge authenticated IPC. Use the existing approved architecture; do not restart the product design.

Implement B2 test-first:
- single-instance Host;
- explicit named-pipe DACL;
- peer validation;
- versioned bounded framing with size/count/time quotas;
- Bridge;
- bounded queue and atomic journal;
- status/cancellation;
- IPC fuzz/adversarial harness.

Preserve the hard boundaries: Explorer trigger only; Host never parses untrusted media or loads codec/plugin code; probing/conversion belongs to disposable restricted workers; no raw executable commands in presets/IPC; no silent strict-isolation downgrade; workers write private staging only; final output remains transactional; numbered-copy remains default; signing private keys never enter the workspace.

Do not implement FFmpeg/WIC execution before the B2/B4 containment foundation is qualified. At tranche end synchronize VERSION/README/CHANGELOG/backlog/status/handover/evidence, regenerate source/release SBOM and SHA-256 authority, run exact-head CI, and package the next complete workspace.
