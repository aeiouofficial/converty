# Converty Continuation Prompt

Continue development directly in the GitHub repository:

`https://github.com/aeiouofficial/converty`

The current delivered authority is `Converty 0.1.0-dev.8`. If I also provide `Converty_0.1.0-dev.8_full_workspace.zip`, treat the repository plus that verified package as the same tranche authority.

Read these first, in order:
1. `docs/HANDOVER_NEXT_AGENT.md`
2. `machine-readable/handover_state.json`
3. `machine-readable/build_evidence.json`
4. `docs/development/IMPLEMENTATION_STATUS.md`
5. `docs/TASK_BACKLOG.md`
6. `docs/Converty_Master_Build_Plan.md`
7. `docs/SECURITY_THREAT_MODEL.md`
8. `docs/TEST_AND_RELEASE_GATES.md`
9. `docs/security/B2_SERVER_AUTH_GATE.md`
10. `docs/supply-chain/SBOM_POLICY.md`
11. `docs/supply-chain/RELEASE_SIGNING_POLICY.md`
12. `docs/superpowers/plans/2026-08-25-b2-dev8-bridge-startup.md`

Continue immediately from the evidence-backed state; do not restart the architecture or ask me to re-specify locked decisions. The next delivered workspace must be `0.1.0-dev.9`, remain inside B2, and again end with a complete verified full-workspace ZIP plus synchronized handover/evidence/checksums and a new copy-paste continuation prompt.

Dev.8 adds the trusted Bridge-to-Host activation tranche. `TrustedHostPath` derives only fixed `Converty.Host.exe` from an absolute existing non-reparse installation directory. `InstalledHostProcessLauncher` is the only approved Bridge process-start site and uses no shell, no caller-provided executable/arguments, hidden/no-console startup, and the trusted install directory. `BridgeSubmissionCoordinator` first tries the existing Host, launches at most once only after connect-stage unavailability, retries within a maximum 30-second startup deadline with delay capped at one second, propagates protocol/application failures without launch, and honors cancellation. The behavior qualification recorded in `machine-readable/build_evidence.json` reached 15/15 locked restore, zero vulnerable-result packages, zero-warning/zero-error Release build, 129/129 managed tests, contract/static gates at the behavior head, and native topology smoke.

B2 is NOT fully closed. Keep B3 blocked and continue B2 test-first. Priority order for dev.9:
1. Implement reciprocal connected-server identity verification before Bridge writes any application request frame. Bind it to the actual Windows installer/signing/package authority; do not invent a shared secret. Fixed path, pipe name, and same-user ownership alone are not sufficient authentication.
2. Revalidate server identity for every new pipe session and fail closed for a wrong, unverifiable, raced, or squatted server. Production signing claims require real signed artifacts and signer/timestamp evidence; unsigned CI binaries must never be mislabeled as signed-server evidence.
3. Decide and test whether status/cancel become explicit bounded versioned IPC operations. Continue rejecting unknown operation types, duplicate JSON members, future versions, malformed GUIDs, oversized payloads, and executable-text fields.
4. Expand executable B2 adversarial coverage for Host restart during queued work, orphan/corrupt journal cases, replay after restart, duplicate RequestId, disconnect before completion/acknowledgement, partial frame/header, oversized declared frame, connect/read/write timeouts, unauthorized SID, fake/squatted server, Host launch failure, second Host instance, and queue/journal failure with no partial in-memory mutation.
5. Close B2 only when the full process-start/server-auth/replay/restart acceptance matrix is executable and green. Only then may a later tranche begin B3 native Explorer `IExplorerCommand` integration.

Preserve non-negotiable boundaries: Explorer trigger only; Host never parses untrusted media or loads codec/plugin code; probing/conversion belongs to disposable restricted workers; ordinary local conversion has no network requirement; IPC uses same-user ACL plus peer validation and bounded/versioned framing; presets/IPC never carry executable commands or pass-through argument vectors; provider options are typed/whitelisted; strict isolation never silently downgrades; workers later write only private staging; Host later validates then atomically commits; numbered-copy remains the safe default; signing private keys never enter the repository/workspace.

Do not implement FFmpeg/WIC/media-provider execution before B4 containment is qualified. Do not weaken existing DACL, peer validation, frame bounds, queue/journal limits, strict JSON, startup limits, or process-start confinement to make the remaining work easier.

Report only gates actually executed. At tranche end synchronize VERSION/README/CHANGELOG/backlog/status/handover/machine-readable evidence, regenerate source/release SBOM plus `machine-readable/package_manifest.json` and `SHA256SUMS.txt`, require zero generated diff on the qualified exact head, build the workspace ZIP twice deterministically, reopen it and verify CRC/embedded hashes/exclusions, update GitHub, and provide the verified `Converty_0.1.0-dev.9_full_workspace.zip` plus the next handover prompt.
