# CONVERTY — CONTINUATION HANDOVER
# PRODUCT-FIRST ROADMAP — FINISH 0.1.0-dev.10 AUTHORITY CLOSURE

Continue development directly in:
https://github.com/aeiouofficial/converty

Repository branch: `dev/0.1.0-dev.10-b4`
Frozen pre-dev.10 main: `13ed46bcb5cb02f33965dace4adc5a3fb25e87fd`
Immutable dev.10 B4 behavior head: `f221563c790057344a94b4e60c309d4512a77c38`
Behavior run: `33028554361`
Managed job: `98375493893`
Static job: `98375494099`

B4 BEHAVIOR IS QUALIFIED; DEV.10 DELIVERY AUTHORITY IS NOT YET FROZEN.
Next development tranche after successful dev.10 authority closure: `0.1.0-dev.11`.

Behavior evidence on Windows Server 2025 / windows-2025-vs2026 / .NET SDK 10.0.400:
- 18/18 locked restore PASS
- vulnerability audit PASS; 0 vulnerable-result packages
- Release build PASS; 0 warnings; 0 errors
- native Explorer PASS
- pinned dev FFmpeg/ffprobe 9.0.1 PASS
- unsigned MakeAppx package PASS
- direct DLL Invoke PASS
- package registration + COM activation/Invoke PASS
- actual Bridge → Strict EngineWorker → FFmpeg product smoke PASS
- Unicode/metacharacter input, source/external destination preservation, numbered output PASS
- ffprobe MP3 exactly 320000 bit/s PASS
- managed tests 190/190 PASS, 0 skipped
- static tests 66/66 PASS
- contract vectors 5/5 PASS
- strict output-growth termination canary PASS

B4 includes private staging, worker/provider migration, suspended native launch, explicit inherited handles, Job Object kill-on-close and finite process/memory/CPU/time/output ceilings, zero-capability AppContainer strict isolation, application read/execute + staging read/write ACLs, reparse rejection, outside-scope-write denial, loopback-network denial, descendant cleanup, and explicit Strict/Compatibility with no silent fallback. Conversion output default is 8 GiB; hard configured max is 16 GiB.

Direct strict-canary evidence: run `33027104465`, managed `98370929641`, static `98370929814`.

The behavior run built two byte-identical ZIPs:
SHA-256 `a0edd6e15a63d71cc2ef493ef33f6bb6e3f0b16ee0d8f484ebc981b800f749de`
369035 bytes
328 files
It then failed only because tracked generated package/SBOM/hash authority was stale. That is expected before authority closure.

FIRST TASK:
Finish dev.10 authority closure. Regenerate source/release SBOM, package manifest and SHA256SUMS from the synchronized dev.10 tree; commit generated authority separately; require zero generated diff and exact-head CI proving all behavior gates plus deterministic ZIP twice, CRC, embedded package-manifest bytes/hashes, SHA256SUMS, exclusion policy and verified delivery upload. Record the fully-green authority qualification using the repository's non-self-referential evidence-freeze pattern, regenerate/requalify if the evidence update changes manifests, then consider merging reviewed dev.10 authority to main.

DO NOT CHANGE THE FROZEN B4 BEHAVIOR UNLESS AN EVIDENCE-BACKED FAILURE REQUIRES IT.

REMAINING SHIPPING BLOCKERS AFTER AUTHORITY CLOSURE:
- real headed Windows 11 modern Explorer context-menu acceptance/screenshots
- Explorer crash/hang/failure headed matrix
- remaining B2 connected-server anti-squatting/authentication
- final status/cancel wire decision
- replay/disconnect/session acceptance
- production FFmpeg redistribution/license/notices/signature/hash
- signed production MSIX
- clean Windows 11 VM install/update/uninstall
- final security/fuzz/chaos/release audit

NON-NEGOTIABLES:
- Explorer trigger-only and cheap/bounded; no media parsing/network/codec/plugin execution in Explorer.
- Host never parses hostile media or loads codec/plugin code.
- Production media parsing/conversion only in disposable restricted workers.
- Strict local conversion has no network.
- No raw commands/pass-through FFmpeg args from Explorer/user/IPC; typed checked-in presets only.
- No shell.
- Fixed app-local worker and FFmpeg paths; reject unsafe reparse-point substitution.
- Worker gets private staging, not final publication destination.
- Never overwrite input/external destination; numbered no-overwrite publication remains transactional/race-safe.
- Explicit Strict/Compatibility only; never silently fall back from Strict.
- Signing private keys never enter repo/workspace.
- Report only executed gates.
- Gyan FFmpeg is development qualification input only, not production redistribution approval.

MANDATORY RECURSIVE HANDOVER RULE:
At the end of your work — whether complete, partial, or blocked — you MUST produce a new full-context copy-paste handover prompt for the NEXT agent/new chat. It must contain:
- exact repository and working branch/main HEAD SHA;
- exact immutable qualification SHA and GitHub Actions run/job IDs;
- what you changed and why;
- tests and acceptance gates actually executed with exact results;
- remaining shipping blockers and explicit unverified claims;
- the single highest-priority next task;
- all non-negotiable product/security constraints;
- and this exact recursive rule requiring that next agent to hand over again.
Repeat that handover chain at every tranche until Converty is actually shipped.
