# dev.22 local qualification — 2026-09-24

Repository: `aeiouofficial/converty`. Development baseline: `ca018a2cdafcfef9e4198a0aa721b6fa66464a74` (tree `f589ce536e95f470789ab4a146af904fe54b62de`). Frozen `main` and qualified dev.21 were not modified.

## Reproduced defects and corrections

- On this non-elevated Windows 11 laptop, two existing reparse-point regressions failed while creating privileged directory symlinks, before reaching the security assertions (393/395 tests passed). Replaced test-only symlink fixtures with ordinary directory junctions, checked the reparse attribute and preserved the expected security rejection. Subsequent test run: **395/395 PASS**, 0 skipped.
- Windows-generated release SBOM gained CRLF in every line, differing from committed Linux-generated bytes. Added four-member LF regression: **RED 1 failure/3 passes** on the release SBOM. Explicit newline handling in all three deterministic generators: **GREEN 4/4**; full Python static suite: **168/168 PASS**.
- Raw contract vectors **5/5 PASS**; repository static verifier, release input preflight and NuGet vulnerability audit **PASS** (19 projects / 19 frameworks / 0 vulnerable-result packages).
- Release managed build **0 warnings / 0 errors** with repository-pinned SDK 10.0.400, installed strictly under `D:\converty\_toolchain\dotnet`.
- Native MSVC Explorer DLL and registration smoke executable compiled successfully on the laptop with Visual Studio 2022 Build Tools.

## Package and distribution checks

- Native MSVC Explorer build and unsigned development MSIX layout/schema validation **PASS** without bundling a converter. This is not an execution/COM/product conversion acceptance or a signed release.
- The exact development FFmpeg download failed its mandatory digest gate: expected 192925997 bytes and SHA-256 `fe372180f20e7f9bfa3d9a481b2b1b98c8296178d8265552608736637ea6b3c8`; the network transfer returned only 150933805 bytes with SHA-256 `adb874e999e4bad50f6078d0bf774095f5fe019197786a50fff24c85e0b448af`. The payload was **rejected before extraction**. Real packaged ProbeWorker/COM/Audio36/Image24/Video27/Copy/Remux/Transcode regressions are **NOT VERIFIED on this amended tree** (previous GitHub CI belongs to the earlier subject).
- Deterministic workspace ZIP generation deliberately reads **committed Git HEAD**, not uncommitted working-tree files. A pre-commit verification correctly rejected its authority-member mismatch; final ZIP validation must run after committing the fully generated authority.

## Evidence and scope

Local managed log: `D:\converty\_temp\managed-test-20260924.log`. All Converty-owned source, scratch, build, download, cache and logs in this block are under `D:\converty`. No GitHub Actions run was requested; previous Actions qualification belongs to the earlier immutable subject and does not qualify this change.

Production release evidence is **OPEN**: actual signed package/B2, production FFmpeg provenance and redistribution, live main governance, clean headed Windows 11 acceptance, final fuzz/chaos/security/end-user and UX/Plugin SDK gates. Local development qualification is not production sign-off.
\n